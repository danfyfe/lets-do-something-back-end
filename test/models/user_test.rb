require '../test_helper'

class UserTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end

  # test "should not save User without username" do
  #   user = User.new
  #   assert_not user.save, "Saved the User without a username"
  # end
  #
  # test "shouhld not save User without an image" do
  #   user = User.new
  #   assert user.save, "Saved the user without an image"
  # end

end
